# Database Configuration
# Choose one of the following database providers:

# Turso (SQLite-compatible, edge database)
# DATABASE_URL=libsql://[database-name].turso.io?authToken=[your-auth-token]

# Neon (PostgreSQL)
# DATABASE_URL=postgresql://[user]:[password]@[host].neon.tech/[database]?sslmode=require

# PlanetScale (MySQL)
# DATABASE_URL=mysql://[user]:[password]@[host].pscale.io/[database]?sslmode=require

# Local SQLite (for development)
DATABASE_URL=file:./dev.db

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Authentication (if using NextAuth.js)
# NEXTAUTH_SECRET=your-secret-key-here
# NEXTAUTH_URL=http://localhost:3000

# Optional: Analytics
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=your-analytics-id
